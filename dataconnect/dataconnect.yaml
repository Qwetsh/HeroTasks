specVersion: "v1beta"
serviceId: "gamify-taches"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "gamify-taches-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
